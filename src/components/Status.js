import React, { Component } from 'react'

// const fetchedData = axios.get('https://api.covid19api.com/summary').then(data=>console.log(data))

export default class Status extends Component {
    render() {
        return (
            <div>
         not yet
            </div>
        )
    }
}
